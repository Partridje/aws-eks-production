# Terraform Workspace Root
# This is not a module - it's the workspace root containing:
# - modules/     : Reusable Terraform modules
# - environments/: Environment-specific configurations (dev, prod)
# - bootstrap/   : Backend infrastructure setup

# No resources are defined here
# Use environment-specific configurations in environments/dev or environments/prod
